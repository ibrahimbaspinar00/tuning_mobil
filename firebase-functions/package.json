{
  "name": "tuning-app-functions",
  "version": "1.0.0",
  "description": "Firebase Functions for Tuning App",
  "main": "index.js",
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^4.8.0",
    "nodemailer": "^6.9.0"
  },
  "engines": {
    "node": "22"
  }
}
